
services:
  - type: web
    name: token-json-builder
    env: python
    region: singapore
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run simple_streamlit_token_builder.py --server.port=$PORT --server.address=0.0.0.0"
